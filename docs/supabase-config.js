// GitHub Actions replaces these empty values in the deployed Pages artifact.
// The anon/publishable key is intentionally browser-visible; data security is enforced by RLS.
window.COIN_RECOMAND_SUPABASE = Object.freeze({
  url: "",
  anonKey: "",
});
